apiVersion: v1
kind: Service
metadata:
  name: ai-request-handler-svc
  namespace: decidim-ai
spec:
  selector:
    app: ai-request-handler
  ports:
   - name: http
     port: 80
     protocol: TCP
     targetPort: http